<h1>Editing Review</h1>

<%= render 'form' %>

<% if @review.startup.nil? %>
  <%= link_to 'Back', reviews_path %>
<% else %>
  <%= link_to 'Back', @review.startup %>
<% end %> |
<%= link_to 'Show', @startup %>
